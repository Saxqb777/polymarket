[build]
builder = "nixpacks"

[deploy]
startCommand = "python main.py --now"
restartPolicyType = "never"

[[cron]]
schedule = "0 16 */2 * *"   # Every 2 days at 16:00 UTC = 20:00 Gulf (UTC+4)
command = "python main.py --now"
